# hospital-appointment-system
A frontend-only Healthcare Appointment Booking System built using HTML, CSS, and Vanilla JavaScript, implementing the First-Come, First-Served (FCFS) scheduling principle.
The system manages patient appointments, queue handling, slot conflicts, and automatic promotion — all stored locally using the browser’s localStorage.
